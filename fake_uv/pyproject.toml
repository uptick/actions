[project]
name = "fake-uv"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.3.3",
]

[tool.uv]
dev-dependencies = [
    "ruff>=0.7.2",
]
